# Delegation catalog (reference only)

Use the lists below to choose `skills` and `tools` for each `spawn`. You cannot call executor tools on the main path.

### Common spawn profiles

| Stage | `tools` | `skills` |
|-------|---------|----------|
| Literature / methods | `rag_search`, `read_file`, `grep` | `markdown`, `zotero` |
| Write docx (create + verify) | `mcp_docx-mcp_create_from_markdown`, `mcp_docx-mcp_open_document`, `mcp_docx-mcp_get_document_info`, `mcp_docx-mcp_get_headings`, `mcp_docx-mcp_search_text` | `docx` |
| Revise docx | `mcp_docx-mcp_open_document`, `mcp_docx-mcp_save_document`, … edit tools … | `docx` |

## Skills

{{ skills_summary }}

{{ tools_section }}
