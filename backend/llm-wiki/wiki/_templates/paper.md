# （论文标题，与 PDF 文献一致或略作可读化）

> **路径约定**：`wiki/` 与 **`raw/markdown/`** **目录树镜像**（同相对路径、同主文件名 `.md`）。解析流水线一般为 `raw/pdf/...pdf` → `raw/markdown/...md` → 本 `wiki/...md`。  
> **可追溯正文**：结论须能在 **`raw/markdown/...`** 对应段落核对；`raw/pdf` 仅作转换输入，不作 wiki 条目的主引用路径。

## 文献实体与元信息

- 作者、年份、venue、任务与贡献一句话
- 数据集 / 评测指标（如实摘录）

## 概念与方法

- 核心定义、方法管线、与相近工作的关系

## 主题要点（本篇可支撑）

- 仅写本篇证据足以支撑的结论；领域级综述放在 `wiki/_synthesis/`

## 对比与数据（以本篇为主）

- 关键表格或数字（须带来源段落或图表定位）
- 若对比涉及多篇，在 `wiki/_synthesis/` 建表并在本节链接过去

## 概述与导航

- 本文在更大研究主题中的位置；相关 `wiki/**/*.md` 与 `_synthesis/` 链接

## 来源

- **必填**：与本页镜像的 **`raw/markdown/...`** 相对路径（相对于 `backend/llm-wiki/`），例如 `raw/markdown/主题/某篇.md`（与 `wiki/主题/某篇.md` 除根目录外路径一致）。  
- **可选**：尚未生成 `.md`、仅能读 PDF 时，可在本节备注「待解析：`raw/pdf/...`」，生成 markdown 后改为仅保留上一行。
